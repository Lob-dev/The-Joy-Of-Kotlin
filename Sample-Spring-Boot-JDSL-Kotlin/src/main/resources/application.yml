spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/sample
    username: root
    password: 1234!!
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      show_sql: true
      format_sql: true
    open-in-view: false